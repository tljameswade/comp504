package model.paint.strategy;

import model.paint.UprightImagePaintStrategy;

public class BombPaintStrategy extends UprightImagePaintStrategy {

	/**
	 * Constructor that loads the Bomb image
	 */
	public BombPaintStrategy() {
		super("images/bomb.png", 0.8);
	}
}
